# Ruby Mock Api Service

## Usage

Create a folder in "apps" folder then copy over the samples from "example" folder.

## Start

To start the app

```shell
bundle
./start.sh
```

Then go to (Localhost)[http://localhost:8000]
